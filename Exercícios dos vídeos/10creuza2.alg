algoritmo "Nome do Programa"

var
convert, convertido: real

inicio
    escreva("Quantos dólares eu posso ter se tiver R$")
        leia(convert)
    escreval(convert)

    convertido <- convert / 4.80
    escreva("Então terei $", convertido)

fimalgoritmo