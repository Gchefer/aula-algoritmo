algoritmo "Contador repita"

var
c, n, R: inteiro
inicio
c <- 1
Escreva("Qual será o multiplicador? ")
    leia(n)
        escreval(n)
repita
     R<-c*n
    escrevaL(N, " x ", c, " = ", R)
    c<-c+1
   
ate(c>10)
fimalgoritmo